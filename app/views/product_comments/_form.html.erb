<%= form_with(model: product_comment) do |form| %>
  <% if product_comment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(product_comment.errors.count, "error") %> prohibited this product_comment from being saved:</h2>

      <ul>
        <% product_comment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :product_id, style: "display: block" %>
    <%= form.text_field :product_id %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :comment, style: "display: block" %>
    <%= form.text_area :comment %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
